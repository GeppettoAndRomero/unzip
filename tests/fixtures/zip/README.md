# Test fixture

`sample.zip` is generated in-repo (Python `zipfile`) with:

- `readme.txt` — a plain root file
- `docs/notes.txt` and `docs/sub/deep.txt` — nested-folder files
- `メモ.txt` — a UTF-8 filename (language-encoding flag / bit 11 set)
- `images/` — an explicit empty-directory entry

5 entries total (4 files + 1 directory). No third-party content.
